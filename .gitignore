node_modules/
build/
dist/
*.apk
*cookiejar*
android-lifeops/.gradle/
android-lifeops/app/build/
android-lifeops/**/build/
android-lifeops/**/*.apk
android-lifeops/**/*.png
tools/vendor/
coverage/
.DS_Store
*.log
.env*
!.env.example

# --- Protect private telemetry + tooling cruft (do not commit) ---
.sentinel-lifeops/
.review-baseline/
.snapshot-pre-situations/
.antigravitycli/
artifacts/
claude-auth-login-*.txt

# Android build + signing
.gradle/
local.properties
*.keystore
*.jks
captures/

# Built web bundle synced into the APK (generated by npm run android:sync-assets).
# It inlines VITE_* env (incl. the ingest token) — never commit it as source.
android-lifeops/app/src/main/assets/web/
